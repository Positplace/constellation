TODO

Constellation view need some re-thinking for multiplayer, it proabaly should be per player.

Not sure if all nebula types are rendering well

Spaceship Physics: Spaceships aren't yet being managed by the server - they're still updated client-side. For full synchronization, you'd want to move the spaceship update loop to the server.

Connection Error Handling: Add better error handling and retry logic for when server is offline.
